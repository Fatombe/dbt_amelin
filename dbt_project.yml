name: 'dbt_course'
version: '1.0.0'
profile: 'dbt_course'
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"



models:
  dbt_course:
    +materialized: view

seeds:
  dbt_course:
    schema: seeds
