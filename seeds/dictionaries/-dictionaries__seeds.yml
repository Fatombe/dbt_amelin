seeds:
  - name: city_region
    config:
      column_types:
        city: varchar(50)
        region: varchar(50)
        updated_at: timestamp
      delimiter: ","
    columns:
      - name: city
        data_tests:
          - not_null
          - unique